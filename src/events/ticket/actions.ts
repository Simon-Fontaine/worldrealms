import { SchemaTicket } from "../../types";
import { cleanUsername } from "../../utils/user";
import { ButtonInteraction } from "discord.js";

const ticketClose = (interaction: ButtonInteraction) => {
  //   ticket.closed = true;
  //   ticket.closed_id = interaction.user.id;
  //   ticket.closed_username = cleanUsername(interaction.user);
};
